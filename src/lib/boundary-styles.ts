// Central boundary design tokens. Colors are defined in src/app/globals.css
// as --border (section separators & container outlines) and --input (form
// element borders) so dark/light behavior stays consistent and future changes
// only need to touch one place.
export const FORM_FIELD_BORDER = "border-input";

export const SECTION_SEPARATOR_BORDER = "border-border";
